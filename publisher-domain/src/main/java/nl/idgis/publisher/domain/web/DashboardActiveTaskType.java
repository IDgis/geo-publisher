package nl.idgis.publisher.domain.web;

import nl.idgis.publisher.domain.NotificationType;

public enum DashboardActiveTaskType implements NotificationType {
	HARVESTER_STARTED,
	IMPORTER_STARTED
}
